let electron,path,fileURLToPath;_66b‍.w("electron",[["default",["electron"],function(v){electron=v}]]);_66b‍.w("path",[["default",["path"],function(v){path=v}]]);_66b‍.w("url",[["fileURLToPath",["fileURLToPath"],function(v){fileURLToPath=v}]]);




const __filename = fileURLToPath(_66b‍._.meta.url);
const __dirname = path.dirname(__filename);
const app = electron.app;
const BrowserWindow = electron.BrowserWindow;
var width, height;


function createWindow() {
	const win = new BrowserWindow({
		width: width,
		height: height,
		icon: __dirname + './resources/resume.PNG',
		webPreferences: {
			preload: path.join(__dirname, "preload.mjs"),
			nodeIntegration: true
		},
	});

	win.loadFile("./www/index.html");
	win.maximize();
}

app.whenReady().then(() => {
	width, height = electron.screen.getPrimaryDisplay().workAreaSize;
	createWindow();

	app.on("activate", () => {
		if (BrowserWindow.getAllWindows().length === 0) {
			createWindow();
		}
	});
});

app.on("window-all-closed", () => {
	if (process.platform !== "darwin") {
		app.quit();
	}
});

electron.ipcMain.on("testFunction", (event, data) => {
	_66b‍.g.console.log(data);
});