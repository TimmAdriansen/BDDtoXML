let electron,path,fileURLToPath;_380‍.w("electron",[["default",["electron"],function(v){electron=v}]]);_380‍.w("path",[["default",["path"],function(v){path=v}]]);_380‍.w("url",[["fileURLToPath",["fileURLToPath"],function(v){fileURLToPath=v}]]);




const __filename = fileURLToPath(_380‍._.meta.url);
const __dirname = path.dirname(__filename);
const app = electron.app;
const BrowserWindow = electron.BrowserWindow;


function createWindow() {
	const win = new BrowserWindow({
		width: 800,
		height: 600,
		webPreferences: {
			preload: path.join(__dirname, "preload.mjs"),
		},
	});

	win.loadFile("./www/index.html");
}

app.whenReady().then(() => {
	createWindow();
  
	app.on("activate", () => {
	  if (BrowserWindow.getAllWindows().length === 0) {
		createWindow();
	  }
	});
  });
  
app.on("window-all-closed", () => {
	if (process.platform !== "darwin") {
		app.quit();
	}
});